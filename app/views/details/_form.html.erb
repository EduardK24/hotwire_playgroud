<%= form_with(model: [@person, @detail], local: true) do |form| %>
  <% if @detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@detail.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
        <% @detail.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :person_title %>
    <%= form.label :age %>
    <%= form.text_field :age, id: :person_age %>
    <%= form.label :phone %>
    <%= form.text_field :phone, id: :person_phone %>
    <%= form.label :email %>
    <%= form.text_field :email, id: :person_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
